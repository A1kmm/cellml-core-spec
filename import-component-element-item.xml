<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE article SYSTEM "-//OASIS//DTD DocBook XML">
<section id="import-component-element">
  <title>The import component element information item</title>
  <para>
    Import component element information items (referred to in this specification as
    import component elements) are element information items in the CellML 1.1
    namespace with local name equal to component, which appear as children of
    import elements.
  </para>
  <section>
    <title>Specific information items</title>
    <orderedlist>
      <listitem>
        <para>
          Every import component element MUST contain a name attribute in the empty
          namespace. The value of the name attribute MUST be a valid CellML
          identifier. The value of the name attribute MUST NOT be identical to
          the name attribute of any other import component or component element in the
          CellML Infoset.
        </para>
      </listitem>
      <listitem>
        <para>
          Every import component element MUST contain a component_ref attribute in the
          empty namespace. The value of the component_ref attribute MUST be a valid
          CellML identifier. The value of the component_ref attribute MUST
          match the value of the name attribute on a component or import component
          element in the imported CellML Infoset.
        </para>
      </listitem>
    </orderedlist>
  </section>
</section>
