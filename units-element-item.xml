<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE article SYSTEM "-//OASIS//DTD DocBook XML">
<section id="units-element">
  <title>The units element information item</title>
  <para>
    Units element information items (referred to in this specification as
    units elements) are element information items in the CellML 1.1
    namespace with local name equal to units, and with either a component
    or model element as their parent.
  </para>
  <section>
    <title>Specific information items</title>
    <orderedlist>
      <listitem>
        <para>
          Every units element MUST contain a name attribute in the empty
          namespace. The value of the name attribute MUST be a valid CellML
          identifier. Where the parent of the units element is a model element,
          the value of the name attribute MUST NOT be identical to the name
          attribute of any other units element child of that model element,
          or of any import units element in the CellML Infoset. Where the parent
          of the units element is a component element, the value of the name
          attribute MUST NOT be identical to the name attribute of any other
          units element child of that component element. In any case, the value
          of the name attribute MUST NOT be equal to a cell in the name column
          of <xref linkend='units-table' />.
        </para>
      </listitem>
      <listitem>
        <para>
          A units element MAY contain one or more unit element children
          (<xref linkend="unit-element" />).
        </para>
      </listitem>
    </orderedlist>
  </section>
</section>
