<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE article SYSTEM "-//OASIS//DTD DocBook XML">
<section id="connection-element">
  <title>The connection element information item</title>
  <para>
    Connection element information items (referred to in this specification as
    connection elements) are element information items in the CellML 1.2
    namespace with local name equal to connection.
  </para>
  <section>
    <title>Specific information items</title>
    <orderedlist>
      <listitem>
        <para>
          Every connection element MUST contain a component_1 attribute in the
          empty namespace.  The value of the component_1 attribute MUST
          be a valid CellML Identifier. The value of this attribute MUST be equal
          to the name attribute on a component or import component element in
          the CellML Infoset.
        </para>
      </listitem>
      <listitem>
        <para>
          Every connection element MUST contain a component_2 attribute in the
          empty namespace.  The value of the component_2 attribute MUST
          be a valid CellML Identifier. The value of this attribute MUST be equal
          to the name attribute on a component or import component element in
          the CellML Infoset.
        </para>
      </listitem>
      <listitem>
        <para>
          Every connection element MUST contain one or more map_variables
          element (<xref linkend="map-variables-element" />).
        </para>
      </listitem>
    </orderedlist>
  </section>
</section>
